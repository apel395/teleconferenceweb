import{ArrowUpRight,Clock3,FileText,Video}from'lucide-react';import{Link}from'react-router-dom';import type{Article,Service}from'../types';
export function ServiceCard({service,onDetail}:{service:Service,onDetail?:(s:Service)=>void}){const Icon=service.icon;return <article className="service-card"><div className="iconbox"><Icon/></div><span className="service-category">{service.category}</span><h3>{service.title}</h3><p>{service.description}</p><div className="service-meta"><span><Clock3/>{service.duration}</span><span><Video/>{service.method}</span></div><div className="card-foot"><button onClick={()=>onDetail?.(service)}>Lihat detail</button><Link to={'/konsultasi?layanan='+service.slug}>Pilih layanan <ArrowUpRight/></Link></div></article>}
export function NewsCard({article,featured=false}:{article:Article,featured?:boolean}){return <article className={'news-card '+(featured?'featured':'')}><div className="news-visual"><FileText/></div><div className="news-body"><div><span>{article.category}</span> · {article.date}</div><h3>{article.title}</h3><p>{article.summary}</p><Link to={'/informasi/'+article.slug}>Baca selengkapnya <ArrowUpRight/></Link></div></article>}
